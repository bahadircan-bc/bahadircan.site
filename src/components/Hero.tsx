// STUB — owned by Agent A ("Signature"). Hero + Focal Plane + the 3 strata.
// Contract: default export, no props. Self-contained (may add src/content/strata.ts).
export default function Hero() {
  return (
    <section id="top" className="flex min-h-screen items-center px-6">
      <p className="font-mono text-sm text-ash">hero / focal-plane stub</p>
    </section>
  );
}
