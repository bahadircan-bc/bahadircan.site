// STUB — owned by Agent C ("Chrome"). Minimal nav: name + in-page anchors + CV ↗.
// Contract: default export, no props.
export default function SiteHeader() {
  return (
    <header className="px-6 py-5 text-sm text-ash">
      Bahadır Can — header stub
    </header>
  );
}
