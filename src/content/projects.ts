import type { Project } from "@/lib/types";

// STUB — populated by Agent B (authored from the CV, see docs/summary.md).
// One entry per project; `summary` stays a single line for the typeset index.
export const projects: Project[] = [];
